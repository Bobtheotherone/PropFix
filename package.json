{
  "name": "propfix",
  "version": "0.2.0",
  "private": true,
  "main": "src/main.js",
  "description": "PropFix – Electron UI (sliders) talking to the local PropFix server",
  "scripts": {
    "start": "electron ."
  },
  "devDependencies": {
    "electron": "^31.3.0"
  },
  "dependencies": {
    "python-shell": "5.0.0"
  }
}
